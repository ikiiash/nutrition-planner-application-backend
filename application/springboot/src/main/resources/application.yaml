openai:
  api-key: ${OPENAI_API_KEY:}

spring:
  application:
    name: nutrition-planner

  datasource:
    url: jdbc:postgresql://localhost:5433/nutrition_planner
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
    name: nutrition-planner-db

  jpa:
    show-sql: false
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
    mapping-resources:
      - persistence/nutrition-planner-orm.xml

  liquibase:
    change-log: persistence/db/changelog/changelog-master.xml

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

---
spring:
  config:
    activate:
      on-profile: keycloak
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/NUTRITION
          jwk-set-uri: http://localhost:8081/realms/NUTRITION/protocol/openid-connect/certs

---
# Kubernetes / Azure profile — env vars injected by k8s deployment
spring:
  config:
    activate:
      on-profile: kubernetes
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI}
          jwk-set-uri: ${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI}

openai:
  api-key: ${OPENAI_API_KEY:}
